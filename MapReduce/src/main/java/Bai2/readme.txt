B1: Set config input và output file cho file NC_Runner, ở đây khi chạy thử em set input là count_distinct.csv và output là outputBai2
B2: Sau khi chạy ta được folder outputBai2, file part-00000 lưu lại các số và số lần xuất hiện của chúng trong file count_distinct.csv,
    file này dùng để đếm số number khác nhau trong file
B3: Set config input và output file cho file Count_Distinct_Number, ở đây khi chạy thử em set input là outputBai2/part-00000
    và output là outputFinalBai2
B4: Sau khi chạy ta được folder outputFinalBai2, file part-r-00000 lưu lại số number khác nhau cần tìm.